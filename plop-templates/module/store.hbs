import { proxy } from "valtio";
import { useProxy } from "valtio/utils";
interface T {
  text: string;
}
const data: T = {
  text: "{{name}}",
};

export const use{{properCase code}}Store = (): T => useProxy(proxy(data));
